# Set these in Railway's Variables tab for the my-fullstack-app service

# Automatically provided by Railway when you link a Postgres database:
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Set this to "production" in Railway:
NODE_ENV=production

# Optional — your frontend URL if deploying client separately
# FRONTEND_URL=https://kylesuda.com
